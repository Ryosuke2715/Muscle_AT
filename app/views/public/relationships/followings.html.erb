<div class="container" style="margin-top: 100px; margin-bottom: 30px;">
  <div class="row justify-content-center">
    <div class="col-md-3">
      <!-- SideBar -->
      <%= render 'public/shared/customer_profile', customer: current_customer %>
    </div>
    <div class="col-md-9">
      <!-- Control links -->
      <%= render 'public/shared/controll' %>
      <h1 class="text-center">フォロー一覧</h1>

      <ul class="list-group">
        <% @followings.each do |following| %>
          <li class="list-group-item">
            <%= link_to following.name, following %>
            フォロー数: <%= following.followings.count %>
            フォロワー数: <%= following.followers.count %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>